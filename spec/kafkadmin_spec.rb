require 'spec_helper'

describe Kafkadmin do
  it 'has a version number' do
    expect(Kafkadmin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
